<% if @instructors.blank? %>
  <h1>Nenhum professor cadastrado</h1>
<% end %>


<% @instructors.each do |instructor| %>
  <h1><%= link_to instructor.name, instructor %></h1>  
  <p><%= instructor.email%></p>
  <%= link_to 'Excluir',  instructor_path(instructor),
    data: { confirm: 'Você tem certeza que deseja excluir?' }, method: :delete %>
<% end %>

<%= link_to 'Cadastrar Professor', new_instructor_path %>
<%= link_to 'Voltar', root_path %>